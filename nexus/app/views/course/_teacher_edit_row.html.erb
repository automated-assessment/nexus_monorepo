<tr class="nested-fields">
  <td>
    <%= collection_select("course[teaching_team_members_attributes][#{f.options[:child_index]}]",
          :user_id,
          all_users,
          :id,
          :name,
          { prompt: true,
            selected: f.object.user_id },
          { class: 'form-control' }) %>
  </td>
  <td>
    <%= link_to_remove_association f do |link| %>
      <div class="col-md-10 col-md-offset-2">
        <%= icon 'minus-circle' %> Remove
        <hr />
      </div>
    <% end %>
  </td>
</tr>
