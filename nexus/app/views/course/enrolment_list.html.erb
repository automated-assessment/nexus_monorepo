<%= render 'header' %>
<h5>Enrolment List</h5>
<% if @course.students.size > 0 %>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Name</th>
        <th width="20%">Staff?</th>
      </tr>
    </thead>
    <tbody>
      <% @course.students.each do |c| %>
        <tr>
          <th scope="row"><%= c.name %></th>
          <td><%= c.admin? %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
