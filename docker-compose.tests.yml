# Requires Docker v 1.12 or above
# The compose file for running grader tests. To be supplemented with appropriate grader definitons from the grader docker-compose file
version: '2'
services:
  syslog:
    image: voxxit/rsyslog:latest
    ports:
      - "5514:514/udp"
    volumes:
      - ./test-logs:/var/log
  grader_tester:
    build:
      context: grader_tester/
      dockerfile: Dockerfile
    volumes:
      - ./tests/scripts:/test-specs
      - ./tests/repositories:/repositories:ro
    environment:
      NEXUS_ACCESS_TOKEN: test_access
      GIT_PORT: 80
      GIT_HOST: git-server
    depends_on:
      - git-server
      - syslog
    logging:
      driver: syslog
      options:
        syslog-address: "udp://localhost:5514"
        tag: "grader-tester"
  git-server:
    image: cirocosta/gitserver-http
    volumes:
      - ./tests/repositories:/var/lib/git
      - ./tests/scripts/submissions:/var/lib/initial:ro
    depends_on:
      - syslog
    logging:
      driver: syslog
      options:
        syslog-address: "udp://localhost:5514"
        tag: "git-server"
