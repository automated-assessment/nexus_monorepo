<% if current_user.assignments.upcoming.empty? %>
  <p>You have no upcoming assignments.</p>
<% else %>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Title</th>
        <th>Course</th>
        <th width="20%">Starts</th>
      </tr>
    </thead>
    <tbody>
      <% current_user.assignments.upcoming.each do |a| %>
        <tr>
          <th scope="row"><%= link_to a.title, a %></th>
          <td><%= link_to a.course.title, a.course %></td>
          <td>
            <%= pretty_datetime a.start %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
