<%= render 'header' %>
<p>
  <h5>Description</h5>
  <%= @course.description %>
</p>
<% if current_user.enrolled_in?(@course.id) %>
  <hr />
  <%= render 'assignments' %>
<% end %>
<%= render 'enrolment' %>
